/**
 * docProcessorHtml — DEPRECATED (v5)
 *
 * Previously held the inline HTML/JS for the hidden WebView document processor.
 * Removed in v5 — see AutoScanCamera.tsx for the new image-picker approach.
 */

export const DOC_PROCESSOR_HTML = "";
